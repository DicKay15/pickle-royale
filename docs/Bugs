Emails are not sent when a user enters a email id, also there should be an option to invite to group directly, so if a admin adds a player, adds his email id to a group and then invites to group by adding an email id, then the player will receive a mail, clicking on the link they get a prompt on the grop screen, with are you sure you want to join the game and claim you player "XYZ".
